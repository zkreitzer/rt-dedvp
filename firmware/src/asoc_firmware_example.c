/* This is an example/template file in firmware/src/.
 * Files in this directory implement embedded firmware for configuring and
 * controlling the ASOC ASIC and FPGA logic.
 * Replace this file with real firmware modules or use it as a starting point
 * for new source files.
 */

#include <stdint.h>

int main(void) {
    /* TODO: initialize hardware, configure ASOC registers, and start acquisition. */
    while (1) {
        /* TODO: implement run loop or event handling. */
    }
    return 0;
}

